(import (scheme base))(import (united base))(define checks (quasiquote ((((united base) ~check-united-base-000) unquote ~check-united-base-000) (((united base) ~check-united-base-001) unquote ~check-united-base-001))))(display "* ")(display "racket")(newline)(let loop ((checks checks)) (unless (null? checks) (display "** ") (display (caar checks)) (newline) ((cdar checks)) (loop (cdr checks))))