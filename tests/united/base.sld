(define-library (united base)
  (export display newline command-line)
  (import (scheme base)
          (scheme write)
          (scheme process-context))


  (define ~check-000
    (lambda ()
      #t)))
